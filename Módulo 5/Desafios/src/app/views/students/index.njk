{% extends 'layout.njk' %}

{% block content %}

<div class="card">
  {# <div class="head">
    <a href="/student/create" class="aBtn-index">Cadastrar professor</a>
  </div> #}
  <table>
    <thead>
      <tr>
        <th></th>
        <th>Nome</th>
        <th>Aulas ativas</th>
        <th>Ação</th>
      </tr>
    </thead>
    <tbody>
    {% for student in student %}
      <tr>
        <td><img src="{{student.avatar_url}}" class="avatar_img"></td>
        <td>{{student.name}}</td>
        <td>

        </td>
        <td><a class="aBtn-index" href="/students/{{student.id}}">Ver</a></td>
      </tr>
    {% endfor %}
    </tbody>
  </table>
</div>

{% endblock content %}